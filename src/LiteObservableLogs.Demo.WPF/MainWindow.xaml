<Window
    x:Class="LiteObservableLogs.Demo.WPF.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="LiteObservableLogs Demo"
    Width="880"
    Height="520"
    MinWidth="640"
    MinHeight="400"
    mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,12"
            FontSize="14"
            Text="Click the button below to write logs through ILogger; the list below subscribes to Log.Received (WriteTo.Event) for real-time display." />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ScrollViewer
                Grid.Column="0"
                HorizontalScrollBarVisibility="Disabled"
                VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Button
                        Margin="0,0,0,8"
                        Padding="12,8"
                        Command="{Binding LogTraceCommand}"
                        Content="LogTrace" />
                    <Button
                        Margin="0,0,0,8"
                        Padding="12,8"
                        Command="{Binding LogDebugCommand}"
                        Content="LogDebug" />
                    <Button
                        Margin="0,0,0,8"
                        Padding="12,8"
                        Command="{Binding LogInformationCommand}"
                        Content="LogInformation" />
                    <Button
                        Margin="0,0,0,8"
                        Padding="12,8"
                        Command="{Binding LogWarningCommand}"
                        Content="LogWarning" />
                    <Button
                        Margin="0,0,0,8"
                        Padding="12,8"
                        Command="{Binding LogErrorCommand}"
                        Content="LogError" />
                    <Button
                        Margin="0,0,0,8"
                        Padding="12,8"
                        Command="{Binding LogCriticalCommand}"
                        Content="LogCritical" />
                    <Button
                        Padding="12,8"
                        Command="{Binding LogExceptionCommand}"
                        Content="LogException" />
                </StackPanel>
            </ScrollViewer>

            <Border
                Grid.Column="2"
                Padding="8"
                BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
                BorderThickness="1"
                CornerRadius="4">
                <DockPanel LastChildFill="True">
                    <TextBlock
                        Margin="0,0,0,8"
                        DockPanel.Dock="Top"
                        FontWeight="SemiBold"
                        Text="Real-time (Log.Received)" />
                    <ListBox
                        FontFamily="Consolas"
                        FontSize="11"
                        ItemsSource="{Binding LiveLines}"
                        ScrollViewer.HorizontalScrollBarVisibility="Auto"
                        ScrollViewer.VerticalScrollBarVisibility="Auto" />
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
